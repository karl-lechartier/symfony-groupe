{% extends 'base.html.twig' %}

{% block title %}Hello EspaceController!{% endblock %}

{% block body %}
    <h1>Gestion des Espaces</h1>
    <table class="table table-striped">
        <tr>
            <th>Titre</th>
            <th>Description</th>
            <th></th>
        </tr>
        {% for e in espaces %}
            <tr>
                <td>{{ e.nom }}</td>
                <td>{{ e.superficie }}</td>
                <td>{{ e.date_ouverture is empty ? "" : e.date_ouverture|date("d/m/Y") }}</td>
                <td>{{ e.date_fermeture is empty ? "" : e.date_fermeture|date("d/m/Y") }}</td>
                <td>
                    <a href="" class="btn btn-primary">modifier</a>
                    <a href="" class="btn btn-danger">supprimer</a>
                    <a href="" class="btn btn-warning">voir animaux</a>
                    <a href="" class="btn btn-warning">voir enclos</a>
                </td>


                {% endfor %}
    </table>
    <a href="{{path("app_espace_ajouter")}}" class="btn btn-success">Ajouter une catégorie</a>

{% endblock %}
